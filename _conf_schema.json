{
  "response_mode": {
    "type": "string",
    "default": "detailed",
    "description": "解析结果输出模式",
    "hint": "detailed（详细版）或 simple（简洁）"
  },
  "include_description": {
    "type": "bool",
    "default": true,
    "description": "是否包含视频简介",
    "hint": "关闭后解析结果不输出简介"
  },
  "include_cover": {
    "type": "bool",
    "default": true,
    "description": "是否包含封面链接",
    "hint": "关闭后解析结果不输出封面链接"
  },
  "description_max_length": {
    "type": "int",
    "default": 120,
    "description": "简介最大输出长度",
    "hint": "超过该长度将被截断，最小值为 20"
  },
  "bilibili_cookie": {
    "type": "object",
    "default": {},
    "description": "B站 Cookie 配置（用于获取高质量字幕）",
    "items": {
      "sessdata": {
        "type": "string",
        "default": "",
        "description": "SESSDATA",
        "hint": "从浏览器 Cookie 中复制 SESSDATA 值，可提升字幕获取成功率"
      },
      "bili_jct": {
        "type": "string",
        "default": "",
        "description": "bili_jct",
        "hint": "从浏览器 Cookie 中复制 bili_jct 值（可选）"
      }
    }
  },
  "summarize_provider_id": {
    "type": "string",
    "default": "",
    "description": "总结视频的 AI 模型",
    "hint": "指定一个 LLM Provider 用于总结字幕内容。留空则直接返回原始字幕，由外层 Agent 自行总结",
    "_special": "select_provider"
  },
  "stt_provider_id": {
    "type": "string",
    "default": "",
    "description": "音频转文字的 STT 模型",
    "hint": "指定一个 STT Provider 用于字幕不可用时下载音频转录。留空则使用当前会话默认的 STT Provider",
    "_special": "select_provider_stt"
  }
}
